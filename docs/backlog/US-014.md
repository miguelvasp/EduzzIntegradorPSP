# US-014 — Implementar cliente e adapter do Mercado Pago

## 1. Identificação

- **ID:** US-014
- **Título:** Implementar cliente e adapter do Mercado Pago
- **Épico:** Integração com PSPs
- **Tipo:** Integração / Domínio / Ingestão
- **Prioridade:** Alta
- **Status inicial:** Backlog
- **Dependências:** US-001 — Estruturar o projeto base da aplicação; US-002 — Definir a arquitetura modular e convenções de desenvolvimento; US-003 — Implantar convenção de enums, constantes, event types e error codes; US-004 — Configurar gerenciamento de configuração e segredos; US-005 — Definir o modelo canônico de transações multi-PSP; US-009 — Implementar sanitização e proteção de dados sensíveis; US-010 — Implantar logging estruturado com requestId e correlação; US-011 — Padronizar tratamento de erros da aplicação; US-012 — Implementar resiliência de integração externa

---

## 2. História

**Como** time de engenharia responsável pelo agregador de transações multi-PSP  
**quero** implementar o cliente HTTP e o adapter do Mercado Pago  
**para** consumir os dados do PSP, interpretar o contrato externo com segurança e convertê-lo para o modelo canônico interno de transações, parcelas e pagadores.

---

## 3. Contexto de negócio

A aplicação deverá consolidar transações de cartão de crédito de múltiplos PSPs, mantendo uma visão única, auditável e consultável do estado financeiro. O desafio define o Mercado Pago como um dos provedores obrigatórios da integração e fornece como contrato esperado de desenvolvimento os endpoints:

- `GET /v1/payments/search?payment_type_id=credit_card&offset=0&limit=20`
- `GET /v1/payments/:id`

além de um exemplo de payload contendo campos como `id`, `date_created`, `date_last_updated`, `payment_type_id`, `status`, `status_detail`, `transaction_amount`, `net_received_amount`, `installments`, `fee_details` e `payer.identification`. O próprio desafio restringe o escopo a transações de cartão de crédito. fileciteturn13file0

A referência atual do Mercado Pago mantém a API de pagamentos com busca e consulta por ID, e a busca em pagamentos informa retorno dos pagamentos efetuados nos últimos doze meses. citeturn421949search0turn421949search1turn421949search4

Essa User Story estabelece a borda de integração com o Mercado Pago, separando contrato externo e domínio interno. Essa separação é necessária para impedir acoplamento da aplicação ao formato do PSP e garantir consistência na persistência, na reconciliação e na API de leitura.

---

## 4. Objetivo

Implementar a integração com o Mercado Pago em duas camadas complementares:

- **cliente HTTP**, responsável por comunicação segura, resiliente e observável com a API externa;
- **adapter**, responsável por converter o payload do Mercado Pago para o modelo canônico interno.

A implementação deverá preparar a base para:
- sincronização paginada;
- detalhamento por pagamento;
- filtragem explícita de escopo para cartão de crédito;
- normalização de valores, status, pagador e parcelas;
- rastreabilidade e auditoria dos dados recebidos.

---

## 5. Escopo

### 5.1 Dentro do escopo
Esta User Story contempla:

- implementação do cliente HTTP para busca e detalhamento de pagamentos no Mercado Pago;
- definição do contrato técnico do cliente;
- implementação do adapter do payload externo para o modelo canônico;
- leitura e uso seguro de credenciais e configuração do PSP;
- tratamento de autenticação e headers necessários;
- integração com timeout, retry, circuit breaker e logging estruturado;
- filtragem de transações para escopo de cartão de crédito;
- normalização de dados financeiros, temporais e cadastrais;
- preparação para consumo pela camada de sincronização.

### 5.2 Fora do escopo
Esta User Story não contempla:

- implementação do fluxo completo de sincronização incremental;
- persistência definitiva no banco;
- abertura de conflitos e reconciliação funcional;
- implementação da API de consulta;
- suporte a meios de pagamento fora do escopo;
- integração com webhooks do Mercado Pago;
- suporte a criação, captura, cancelamento ou reembolso de pagamentos no PSP.

---

## 6. Descrição funcional

A integração com o Mercado Pago deverá permitir obter pagamentos e interpretar apenas os casos compatíveis com o escopo da aplicação. O sistema deverá:

- consumir a busca paginada de pagamentos;
- consumir o detalhe de um pagamento específico quando necessário;
- identificar se o pagamento pertence ao escopo de cartão de crédito;
- extrair os dados relevantes do pagador, do pagamento, das taxas e do parcelamento;
- traduzir o contrato recebido para o modelo canônico interno;
- descartar, rejeitar ou sinalizar itens não compatíveis com as regras mínimas do produto.

A aplicação não deverá utilizar diretamente o payload do Mercado Pago como modelo interno de negócio.

---

## 7. Descrição técnica

### 7.1 Diretriz técnica

A integração deverá ser construída com separação entre:

- **cliente**: responsável por chamada externa, autenticação, paginação, tratamento técnico da resposta e observabilidade;
- **adapter**: responsável por mapear o contrato externo do Mercado Pago para o modelo canônico da aplicação.

Essa separação deverá seguir a estratégia arquitetural do projeto, utilizando Adapter e integração orientada por módulos.

### 7.2 Endpoints mínimos esperados

A implementação deverá contemplar, no mínimo, os seguintes endpoints lógicos do Mercado Pago:

#### a) Busca de pagamentos
- rota base: `/v1/payments/search`
- parâmetros mínimos esperados: `payment_type_id`, `offset`, `limit`

#### b) Detalhe do pagamento
- rota base: `/v1/payments/{id}`

A busca deverá ser usada como fonte principal da sincronização. O detalhe deverá ser utilizado quando necessário para complementar dados, tratar inconsistências ou enriquecer itens específicos.

### 7.3 Contrato externo mínimo esperado

Com base no contrato do desafio, o adapter deverá estar preparado para interpretar, no mínimo, os seguintes grupos de dados do Mercado Pago:

#### a) Dados do pagamento
- `id`
- `date_created`
- `date_approved`
- `date_last_updated`
- `payment_type_id`
- `status`
- `status_detail`
- `currency_id`
- `transaction_amount`
- `net_received_amount`
- `total_paid_amount`
- `installments`

#### b) Dados de taxas
- `fee_details[].type`
- `fee_details[].amount`
- `fee_details[].fee_payer`

#### c) Dados do pagador
- `payer.id`
- `payer.email`
- `payer.first_name`
- `payer.last_name`
- `payer.identification.type`
- `payer.identification.number`

#### d) Dados de paginação
- `paging.total`
- `paging.limit`
- `paging.offset`

### 7.4 Política de escopo funcional

A integração deverá considerar apenas transações de cartão de crédito. No contexto do Mercado Pago, isso deverá ser avaliado por:

- uso explícito de `payment_type_id=credit_card` na busca;
- validação do campo `payment_type_id` no payload recebido.

Pagamentos que não pertençam ao escopo de cartão de crédito não deverão ser tratados como transações elegíveis à ingestão do produto.

### 7.5 Regras de leitura do payload

A interpretação do payload deverá obedecer às seguintes regras mínimas:

- a busca poderá retornar múltiplos pagamentos e a integração deverá tratar cada item individualmente;
- a ausência de `payer` com dados mínimos exigidos deverá conduzir a rejeição funcional em etapa apropriada;
- a ausência de `installments` ou inconsistência de parcelamento deverá ser tratada conforme regra de negócio da ingestão;
- a leitura deverá ser defensiva e compatível com evolução controlada do contrato externo;
- a estrutura não deverá assumir presença irrestrita de todos os campos opcionais.

### 7.6 Responsabilidades do cliente HTTP

O cliente do Mercado Pago deverá ser responsável por:

- montar as requisições para busca paginada e detalhe;
- aplicar autenticação e headers exigidos;
- respeitar configuração centralizada;
- aplicar timeout e política de resiliência;
- classificar erros técnicos de integração;
- emitir logs estruturados de chamada;
- devolver resposta bruta ou contrato técnico controlado para o adapter.

### 7.7 Responsabilidades do adapter

O adapter do Mercado Pago deverá ser responsável por:

- validar a presença dos blocos mínimos necessários ao mapeamento;
- converter o payload externo em `Transaction`, `Installment` e `Payer` canônicos;
- normalizar moeda, datas, status e valores;
- converter valor monetário para a convenção interna da aplicação;
- calcular ou compor taxas a partir de `fee_details`;
- separar valores externos do contrato dos valores canônicos do domínio;
- propagar informações necessárias para rastreabilidade e auditoria.

### 7.8 Mapeamento canônico mínimo esperado

A adaptação deverá contemplar, no mínimo:

#### a) Transaction
- `externalId` a partir do identificador do pagamento;
- `psp = mercadopago`;
- `status` canônico derivado do payload do Mercado Pago;
- `originalAmount` a partir de `transaction_amount`, convertido para centavos;
- `netAmount` a partir de `net_received_amount`, convertido para centavos;
- `fees` a partir da soma de `fee_details[].amount` ou da diferença financeira coerente, convertido para centavos;
- `installmentCount` a partir de `installments`;
- `currency` a partir de `currency_id`;
- `createdAt` a partir de `date_created`;
- `updatedAt` a partir de `date_last_updated`.

#### b) Payer
- `externalId` a partir de `payer.id`;
- `name` a partir da composição de `first_name` e `last_name`;
- `email`;
- `documentHash` a partir de `payer.identification.number`;
- `documentType` a partir de `payer.identification.type`.

#### c) Installments
A integração deverá preparar a base de parcelas a partir do número de parcelas disponível no payload. Como o contrato simplificado do desafio não traz detalhamento completo individual por parcela no retorno de pagamentos, a estratégia deverá estar documentada para:
- compor parcelas mínimas coerentes com a informação disponível; ou
- marcar a necessidade de enriquecimento adicional quando o contrato não for suficiente para preenchimento completo.

Essa decisão deverá respeitar a regra do produto de rejeitar transações sem parcelas completas quando a completude exigida não puder ser garantida.

### 7.9 Mapeamento de status

Os status externos do Mercado Pago não deverão ser reutilizados diretamente como status internos do domínio. O adapter deverá mapear o estado recebido para o catálogo canônico da aplicação.

Exemplos mínimos de interpretação esperada:
- `approved` com `status_detail=accredited` → `paid`;
- `pending` → `pending`;
- `rejected` → `failed`;
- status não mapeado → `unknown` ou ocorrência equivalente conforme política da aplicação.

A política detalhada de transição será refinada em histórias de reconciliação e merge seguro, mas o adapter deverá produzir valor canônico consistente desde a integração.

### 7.10 Tratamento de dados sensíveis

A integração deverá aplicar a política de proteção de dados sensíveis definida pela aplicação, incluindo:

- hash do documento do pagador;
- não persistência nem propagação indevida do documento em texto puro;
- sanitização do payload antes de armazenamento como evidência;
- redaction de segredos e credenciais em logs de erro.

### 7.11 Tratamento de erros da integração

A implementação deverá distinguir, no mínimo:

- erro de autenticação;
- timeout;
- indisponibilidade temporária;
- contrato inválido;
- payload incompleto;
- item fora de escopo;
- ausência de dados mínimos para adaptação.

Erros técnicos do cliente e inconsistências de mapeamento deverão ser registrados com correlação adequada e sem vazamento de dados sensíveis.

### 7.12 Logging e observabilidade

A integração deverá registrar logs estruturados contemplando, no mínimo:

- PSP;
- operação executada;
- endpoint lógico;
- parâmetros de paginação;
- requestId ou correlationId;
- syncRunId quando aplicável;
- externalId quando conhecido;
- resultado da chamada;
- falha técnica ou falha de adaptação;
- duração da operação.

### 7.13 Estruturas mínimas esperadas

A base deverá prever, no mínimo, os seguintes componentes:

#### a) `MercadoPagoHttpClient`
Responsável por comunicação com a API do Mercado Pago.

#### b) `MercadoPagoTransactionAdapter`
Responsável por adaptar payloads do Mercado Pago para o modelo canônico.

#### c) `MercadoPagoResponseSchema`
Estruturas ou schemas para validação mínima do contrato recebido.

#### d) `MercadoPagoIntegrationMapper`
Estrutura auxiliar ou convenção equivalente para mapeamentos específicos do PSP, quando necessário.

### 7.14 Estrutura esperada no projeto

A implementação deverá ser compatível com estrutura como:

```text
src/modules/psp/infrastructure/clients/mercadopago/
  MercadoPagoHttpClient.ts
  MercadoPagoTransactionAdapter.ts
  mercadopago.schemas.ts
  mercadopago.mappers.ts

docs/architecture/
  mercadopago-integration.md
```

### 7.15 Relação com a sincronização

A implementação desta User Story deverá preparar a base para consumo pela sincronização, permitindo que a camada de sync:

- busque páginas no Mercado Pago;
- obtenha detalhes quando necessário;
- receba dados já normalizados em estrutura canônica;
- classifique rejeições, conflitos e erros em etapas posteriores.

### 7.16 Estrutura documental mínima esperada

A definição desta User Story deverá ser refletida em documentação técnica com arquivo compatível com:

```text
docs/architecture/
  mercadopago-integration.md
```

### 7.17 Artefatos mínimos esperados no projeto

A conclusão desta User Story deverá resultar, no mínimo, na criação ou preparação dos seguintes grupos de artefatos:

```text
src/modules/psp/infrastructure/clients/mercadopago/
  MercadoPagoHttpClient.ts
  MercadoPagoTransactionAdapter.ts
  mercadopago.schemas.ts
  mercadopago.mappers.ts

docs/architecture/
  mercadopago-integration.md
```

---

## 8. Regras e princípios

A implementação desta User Story deverá obedecer aos seguintes princípios:

- separação entre contrato externo e domínio interno;
- filtragem explícita do escopo de cartão de crédito;
- mapeamento canônico consistente;
- integração resiliente e observável;
- proteção de dados sensíveis;
- ausência de hardcode espalhado de valores do PSP;
- tratamento defensivo de payload;
- preparação para sincronização incremental e auditoria.

---

## 9. Entregáveis

- cliente HTTP do Mercado Pago implementado ou preparado conforme escopo;
- adapter do Mercado Pago para o modelo canônico;
- schemas ou validações mínimas do contrato do PSP;
- documentação técnica da integração com o Mercado Pago;
- base pronta para uso pela sincronização.

---

## 10. Critérios de aceite

### 10.1 Critérios funcionais
- A aplicação deverá ser capaz de consumir a busca paginada de pagamentos do Mercado Pago.
- A aplicação deverá ser capaz de consumir o detalhe de um pagamento do Mercado Pago.
- O adapter deverá converter payload compatível do Mercado Pago em modelo canônico interno.
- Apenas transações de cartão de crédito deverão ser consideradas elegíveis ao escopo.
- O tratamento de documento do pagador deverá respeitar a política de hash e não exposição.

### 10.2 Critérios técnicos
- O cliente deverá usar configuração centralizada e política de resiliência da aplicação.
- O adapter deverá desacoplar os nomes e formatos do PSP do domínio interno.
- O mapeamento de status deverá produzir valores canônicos da aplicação.
- A integração deverá possuir logs estruturados e correlação.
- A solução deverá estar preparada para persistência de evidência e uso em sincronização.
- O contrato recebido deverá passar por validação mínima antes da adaptação.

### 10.3 Critérios de qualidade
- A implementação deverá ser legível e modular.
- A documentação deverá explicar claramente a estratégia de leitura e adaptação do payload.
- A base criada deverá reduzir ambiguidade para implementação da sincronização e dos testes.
- A solução deverá suportar evolução do contrato do PSP com impacto controlado.

---

## 11. Mapeamento de testes

### 11.1 Testes unitários
- Validar adaptação de payload válido do Mercado Pago para `Transaction`.
- Validar adaptação de `payer` para `Payer` com hash do documento.
- Validar identificação correta de operação de cartão de crédito.
- Validar mapeamento de status externos para status canônicos.
- Validar rejeição ou classificação adequada de payload sem dados mínimos.

### 11.2 Testes de integração
- Validar chamada da busca paginada do Mercado Pago com parâmetros corretos.
- Validar chamada de detalhe de pagamento.
- Validar tratamento de timeout, autenticação inválida e indisponibilidade.
- Validar logs estruturados da integração.
- Validar sanitização do payload quando persistido como evidência.

### 11.3 Testes de qualidade técnica
- Validar ausência de acoplamento do domínio ao contrato externo do Mercado Pago.
- Validar coerência entre documentação e implementação do adapter.
- Validar uso dos catálogos centrais de status, PSP e error codes.

### 11.4 Evidências esperadas
- cliente HTTP do Mercado Pago presente no projeto;
- adapter do Mercado Pago presente no projeto;
- documentação técnica da integração presente;
- testes cobrindo busca, detalhe, adaptação e erros técnicos;
- base pronta para uso pelo fluxo de sincronização.

---

## 12. Dependências para histórias futuras habilitadas por esta US

A conclusão desta User Story habilita diretamente a execução das seguintes histórias:

- US-015 — Implementar Strategy e Factory para resolução de PSP
- US-016 — Implementar mecanismo acionável de sincronização
- US-017 — Implementar sincronização paginada e incremental
- US-018 — Implementar idempotência de ingestão
- US-019 — Implementar validação de negócio na ingestão
- US-020 — Implementar tratamento isolado de erro por transação
- US-021 — Implementar política de transição de status e merge seguro
- US-038 — Implementar mock server dos PSPs para desenvolvimento e teste
- US-039 — Implementar testes unitários do domínio e dos adapters
- US-041 — Implementar testes end-to-end da API e da sincronização
- US-047 — Documentar modelo de dados, fluxo de sincronização e reconciliação

---

## 13. Riscos

- interpretação incorreta do contrato do Mercado Pago;
- ingestão de itens fora do escopo de cartão de crédito;
- acoplamento excessivo do domínio ao payload externo;
- inconsistência no mapeamento de status e valores;
- tratamento inadequado de dados monetários decimais;
- vazamento de dados sensíveis em logs ou evidência de integração;
- necessidade de retrabalho caso o adapter não seja suficientemente defensivo.

---

## 14. Definição de pronto

A User Story será considerada concluída quando:

- o cliente HTTP do Mercado Pago estiver implementado ou preparado conforme escopo;
- o adapter do Mercado Pago estiver convertendo o contrato externo para o modelo canônico interno;
- a filtragem de escopo para cartão de crédito estiver definida e funcional;
- os critérios de aceite estiverem atendidos;
- houver documentação técnica e evidência automatizada suficientes para suportar a evolução da sincronização e da persistência.

---
